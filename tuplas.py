"""  
Tiene la misma funcion que las listas
con la diferencia de que una vez creada 
no se puede modificar/mutar (insetar, eliminar) en el transcurso del proyecto
Se usa cuando los datos sean constantes y no mutaran
"""

my_tuple = (1,"string", True)

print(my_tuple[0:2])